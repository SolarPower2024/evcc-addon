# The official evcc optimizer image, pinned to a build that matches the evcc
# version of the evcc custom addon. Multi-arch: Home Assistant pulls the arm64
# variant on a Raspberry Pi. Bump the tag together with `version` in
# config.yaml, see DOCS.md.
FROM docker.io/evcc/optimizer:1024dfada3d78846539845a28ee44f20a122d625

LABEL io.hass.type="addon"
